# Checkpoint Placeholder

Store pre-trained weights under `checkpoints/pretrained` and training outputs in
`checkpoints/trained`. See `WINDOWS_DATA_SETUP.md` for a Windows-specific guide
and consult the configuration files in `local_configs` for the expected
filenames.
