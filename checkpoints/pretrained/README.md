# Pretrained Weights Placeholder

Place the downloaded ImageNet and released DFormer / DFormerv2 weights here so
that the configuration files such as `local_configs/SUNRGBD/DFormerv2_B.py`
can load them without changes.
