# Trained Checkpoints Placeholder

When you run `train.sh` or `train.bat`, the scripts will save experiment outputs
under `checkpoints/trained/<DatasetName>`. Keep your fine-tuned models here to
match the expected directory layout.
